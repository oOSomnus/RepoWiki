You are defining the architecture map for a repository. This is not a source
index or a directory classification task.

<CANDIDATE_COMPONENTS>
{potential_core_components}
</CANDIDATE_COMPONENTS>

Choose reader-facing modules for the repository's real subsystems, interfaces,
execution stages, state and storage boundaries, and integrations. Cover the
major distinct responsibilities visible in the candidates. Do not merge
separate responsibilities merely to keep the module count small. Do not create
pages for tests, generated protocol types, isolated helpers, or implementation
trivia unless they form a meaningful system boundary.

Select a few representative component IDs per module. Copy each selected ID
exactly from the input. The component list is evidence selection, not an
exhaustive partition; unselected candidates remain in the analysis graph.

Review every proposed module for decomposition. Use `split` when there are
reader-meaningful child boundaries such as distinct interfaces, execution
stages, state/storage areas, or integrations. Use `retain_leaf` only when the
module is cohesive or no evidence supports a useful split. Set `breadth_risk`
to `low`, `medium`, or `high`; a broad module that remains a leaf must have a
concrete explanation. Do not split only to increase page count, and do not
force a directory-shaped hierarchy.

Module keys become canonical Markdown page names. Use non-empty ASCII keys
containing only letters, digits, `_`, or `-`; put localized wording in the
Markdown title and prose. Name modules by responsibility, not nearest folder.
Build, packaging, and test infrastructure may be one secondary module, but
must not dominate the runtime architecture.

Return only this JSON marker. Include a decomposition review for every module:
<GROUPED_COMPONENTS>
{
  "Module_Name": {
    "path": "src/or/relevant/path",
    "components": ["exact/file/path::Representative"],
    "children": {},
    "decomposition_review": {
      "decision": "split",
      "breadth_risk": "medium",
      "reason": "The module contains distinct request routing and execution stages."
    }
  }
}
</GROUPED_COMPONENTS>

Use `decision: "split"` when a module has children to discover; otherwise use
`decision: "retain_leaf"`. Every selected ID must be copied exactly from the
input. Never invent an ID, silently change an ID, or use a bare symbol name.

{custom_instructions}
