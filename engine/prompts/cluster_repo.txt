You are defining the architecture map for a repository. This is not a source
index and it is not a directory classification task.

<CANDIDATE_COMPONENTS>
{potential_core_components}
</CANDIDATE_COMPONENTS>

Choose a small set of reader-facing architectural modules. A module should
represent a real subsystem, interface, execution stage, state boundary,
storage boundary, or integration surface. Do not create a page for tests,
generated protocol types, isolated helpers, or implementation trivia unless
it forms a meaningful system boundary.

Select only a few representative component IDs per module. Every selected ID
must be copied exactly from the input. It is acceptable, and expected, for
many candidate components to remain only in the analysis graph.

Use names that describe the responsibility a reader needs to understand, not
the nearest directory. Prefer a shallow architecture with a small number of
meaningful modules. Build, packaging, and test infrastructure may be grouped
as one secondary module, but must not dominate the runtime architecture.

Return only this JSON marker. Do not return Markdown:
<GROUPED_COMPONENTS>
{
  "Module Name": {
    "path": "src/or/relevant/path",
    "components": ["exact/file/path::Representative"],
    "children": {}
  }
}
</GROUPED_COMPONENTS>

The components list is an evidence selection, not an exhaustive partition.
Never invent an ID, silently change an ID, or use a bare symbol name.
