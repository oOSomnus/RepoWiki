You are an AI documentation assistant. Your task is to generate a brief overview of the {repo_name} repository.

The overview should explain the repository as an architecture, not as a flat
directory listing. It must include:
- the repository's purpose and major responsibilities;
- an end-to-end request/data-flow architecture visualized by at least one
  grounded Mermaid diagram;
- references to every top-level module documentation page;
- build/run or deployment context when artifact information is supplied.

Provide `{repo_name}` repo structure:
<REPO_STRUCTURE>
{repo_structure}
</REPO_STRUCTURE>

The core modules' documentation is NOT inlined above. Each top-level module carries a `docs_path` field with the absolute path to its documentation file — read those files with your file-reading tools before writing the overview (skip entries whose `docs_path` is null).

Please generate the overview of the `{repo_name}` repository in markdown format with the following structure:
<OVERVIEW>
overview_content
</OVERVIEW>

Use semantic headings such as `## Purpose`, `## Architecture`, `## Data flow`,
and `## How it is built and run` when relevant. Explain the relationships
between modules and provide useful navigation; do not emit only a generated
module list or a root-to-directory diagram.
