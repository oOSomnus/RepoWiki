You are writing an architecture page for the {module_name} module.

<REPO_STRUCTURE>
{repo_structure}
</REPO_STRUCTURE>

<ARCHITECTURE_CONTEXT>
{architecture_context}
</ARCHITECTURE_CONTEXT>

<ARCHITECTURE_FEW_SHOTS>
{few_shot_examples}
</ARCHITECTURE_FEW_SHOTS>

Read every linked immediate child page before writing. Explain the module's
responsibility, its interface to the surrounding system, how its children
compose into one architectural role, and the most important control or data
flow through it.

Write a substantive parent article that explains the relationships between
children, not just their names. Trace how requests/data cross the child
boundaries, identify important interfaces or shared state from the evidence,
and state what remains outside this module. Mention lifecycle or failure
behavior when the source makes it relevant.

Choose headings and ordering based on the module. Do not emit a fixed child
list, directory summary, repeated boilerplate, or a page that only restates
child names.

Include a Mermaid diagram when the module has a meaningful architecture to
show. The diagram should connect real child modules, interfaces, state, or
external dependencies. It must be grounded in the architecture context and
must not be a generic Caller -> Entry -> Core -> Result chain.

Link to every documented immediate child page, but explain the relationships
between those pages in prose. Return Markdown only inside:
<OVERVIEW>
overview_content
</OVERVIEW>
