You are refining the architecture map for the existing module {module_name}.
The goal is to make the module easier to read, not to enumerate every source
symbol.

<MODULE_TREE>
{module_tree}
</MODULE_TREE>

<CANDIDATE_COMPONENTS>
{potential_core_components}
</CANDIDATE_COMPONENTS>

Return child modules only when they represent distinct responsibilities,
interfaces, execution stages, state, storage, or integrations. Keep the tree
shallow. Do not split a cohesive implementation merely because it contains
many functions, tests, generated types, or files.

Child keys become canonical Markdown page names. Use non-empty ASCII keys
containing only letters, digits, `_`, or `-`; put localized wording in page
titles and prose rather than in the key.

Each child may select representative component IDs only. IDs must be copied
exactly from the input. Components that are not selected remain available in
the dependency graph and do not need a documentation page.

Return only this JSON marker:
<GROUPED_COMPONENTS>
{
  "Child Module": {
    "path": "src/or/relevant/path",
    "components": ["exact/file/path::Representative"],
    "children": {}
  }
}
</GROUPED_COMPONENTS>

Do not create a directory-shaped child, do not invent IDs, and do not return a
component inventory disguised as an architecture tree.
