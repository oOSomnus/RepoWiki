Generate comprehensive documentation for the {module_name} module using the provided module tree and core components.

The result must be a source-grounded explanatory Markdown page, not a
directory listing. Explain the module's purpose, responsibilities, control or
data flow, important interfaces, and relationships to neighboring modules.
Use semantic headings and include at least one concrete explanation tied to
the supplied file paths and component names. Add Mermaid diagrams when they
clarify architecture or execution. Do not invent behavior, omit all prose, or
fill the page with a repeated template.

<MODULE_TREE>
{module_tree}
</MODULE_TREE>
* NOTE: You can refer the other modules in the module tree based on the dependencies between their core components to make the documentation more structured and avoid repeating the same information. Know that all documentation files are saved in the same folder not structured as module tree. e.g. [alt text]([ref_module_name].md)

<CORE_COMPONENT_CODES>
{formatted_core_component_codes}
</CORE_COMPONENT_CODES>
