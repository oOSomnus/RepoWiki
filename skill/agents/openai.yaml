interface:
  display_name: "RepoWiki Wiki Generator"
  short_description: "Generate repository wiki artifacts with a host agent"
  default_prompt: "Use $repo-wiki to generate or incrementally update a repository wiki with the Rust CLI."
policy:
  allow_implicit_invocation: true
